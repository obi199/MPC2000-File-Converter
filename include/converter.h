#include <string>

using namespace std;

int convert_any_wav2snd(string);
int convert16Bitwav2snd(string);
string snd2wav(const char*);
string wav2snd(const char*);
string read_write_wav(const char*);